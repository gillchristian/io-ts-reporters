{
  "name": "io-ts-reporters",
  "main": "./target/src/index.js",
  "typings": "./target/src/index.d.js",
  "version": "0.0.4",
  "scripts": {
    "lint": "tslint --project tsconfig.json --type-check",
    "compile": "rm -rf ./target/* && tsc",
    "test": "yarn run compile && yarn run lint && node ./target/tests/index.js",
    "prepublish": "yarn run compile && yarn run lint"
  },
  "dependencies": {
    "fp-ts": "^0.2.1",
    "io-ts": "^0.3.0",
    "tslint": "^5.1.0",
    "typescript": "^2.2.2"
  }
}
